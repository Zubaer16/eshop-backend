TokenResponse:
  type: object
  properties:
    user:
      $ref: './User.yaml#/User'
    accessToken:
      type: string
      description: JWT access token
    refreshToken:
      type: string
      description: JWT refresh token
